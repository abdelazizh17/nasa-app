import 'package:flutter/material.dart';

class SuperEarthPage extends StatelessWidget {
  const SuperEarthPage({super.key});
static String id = 'SuperEarthPage';
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}